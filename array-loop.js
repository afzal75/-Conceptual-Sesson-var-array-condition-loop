var items = ['bag', 'shoes', 'watch', 'bottole', 'computer', 'paper', 'book', 'charger', 'pen', 'mouse', 'keyboard'];

for (var i = 0; i < items.length; i++) {
    var item = items[i];
    console.log(item);
}

